name: Repository Baseline

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  verify-structure:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.13"
      - name: Verify repository baseline
        run: python scripts/verify_repository_baseline.py
