# simulator

Produces deterministic synthetic device, duplicate, reordering, offline and ACK-loss scenarios. It is isolated from production runtime classpaths.

Current status: directory and responsibility baseline. Executable bootstrap is M0 scope.
