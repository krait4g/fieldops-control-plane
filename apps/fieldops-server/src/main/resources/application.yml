spring:
  application:
    name: fieldops-server
  main:
    banner-mode: "off"

server:
  port: ${FIELDOPS_SERVER_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - info
  endpoint:
    health:
      probes:
        enabled: true
