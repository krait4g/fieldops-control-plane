# billing-job

Runs usage aggregation, invoice preview and reconciliation. PostgreSQL usage records are authoritative; Redis counters are not a billing source of truth.

Current status: directory and responsibility baseline. Executable bootstrap is M0 scope.
