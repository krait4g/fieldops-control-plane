# ingestion-gateway

Authenticates devices, validates MQTT/HTTP input and publishes raw telemetry. It does not own history, Redis latest state, rules or operator workflows.

Current status: directory and responsibility baseline. Executable bootstrap is M0 scope.
