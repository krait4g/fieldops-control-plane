logging:
  level:
    root: WARN

server:
  port: 0

spring:
  datasource:
    url: jdbc:h2:mem:gateway;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password: ""

management:
  health:
    redis:
      enabled: false
