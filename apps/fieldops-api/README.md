# fieldops-api

Spring Boot HTTP/SSE edge for registry queries, alarms, incidents, commands and approvals. It owns the HTTP boundary, not MQTT ingestion or direct device protocol access.

Current status: directory and responsibility baseline. Executable bootstrap is M0 scope.
