# automation-worker

Runs offline detection, rule evaluation, alarm/incident workflow and command timeouts through domain/application ports. It cannot bypass the durable command ledger.

Current status: directory and responsibility baseline. Executable bootstrap is M0 scope.
