# Cross-boundary Tests

- `contract-tests/`: OpenAPI, AsyncAPI and JSON Schema compatibility
- `e2e-tests/`: device-to-console and command-to-device workflows
- `performance-tests/`: deterministic load profiles
- `fault-tests/`: restart, latency, duplication, reordering and recovery

Every generated scenario records a version, seed and commit SHA.
